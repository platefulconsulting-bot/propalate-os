# Copy to .env and fill in. Never commit the real .env.

# Supabase project (same one the dashboard uses)
SB_URL=https://mpxcoebmevkekvgsfpzf.supabase.co
SB_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1weGNvZWJtZXZrZWt2Z3NmcHpmIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzU3MTcxOTYsImV4cCI6MjA5MTI5MzE5Nn0.9zAefS71Vo8PeNQiL9517PmziKuSyKTGS6vOw8wvnQE

# How often to poll Supabase (seconds)
POLL_SEC=60

# Pause between sends in milliseconds. 8000-15000 looks more human.
RATE_LIMIT_MS=8000

# Hard daily cap as anti-ban guard. Won't exceed this many sends in a 24h window.
DAILY_CAP=200

# Set DRY_RUN=1 to log what would be sent without actually sending. Useful for first run.
DRY_RUN=0
